import { mapState, mapGetters, mapActions, mapMutations } from './helpers'
import { Store, install } from './store'
export default {
  Store,
  install,
  mapState,
  mapGetters,
  mapActions,
}
export { Store, install, mapState, mapGetters, mapMutations }
